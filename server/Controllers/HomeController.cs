using Microsoft.AspNetCore.Mvc;

public class HomeController : Controller
{
    // No need for HomePage action, let the static file middleware handle it

    // If you have other actions, keep them as needed
}
